name: Go API Cloud Functions
on:
  push:
  pull_request:
    branches:
      - [main]
  
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Get version Go
        run: go version

      